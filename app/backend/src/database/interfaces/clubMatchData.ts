// import Match from '../models/matchs';

export default interface IClubMatchData {
  // totalVictories : Match[] | number,
  // totalLosses : Match[] | number,
  // totalDraws : Match[] | number,
  // totalGames: Match[] | number,
  totalVictories : number,
  totalLosses : number,
  totalDraws : number,
  totalGames: number,
  goalsFavor : number,
  goalsOwn : number,
}
